import { Plus } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { useEstoque } from "@/app/EstoqueContext";
import { Badge, Button, Card } from "@/components/ui";
import { brDateTime, itemNome, localNome } from "@/lib/estoqueView";
export function MovimentacoesPage(){const {db}=useEstoque();const nav=useNavigate();if(!db)return null;return <div className="space-y-5"><div className="flex items-start justify-between"><div><h1 className="text-2xl font-bold">Movimentações</h1><p className="text-sm text-slate-500">Histórico imutável. Correções devem ser compensatórias ou por estorno.</p></div><Button onClick={()=>nav('/movimentacoes/nova')}><Plus size={17}/>Nova</Button></div><Card className="overflow-x-auto p-0"><table className="w-full text-left text-sm"><thead className="bg-emerald-950 text-white"><tr>{["Data e hora","Tipo","Item / lote","Fluxo","Quantidade","Responsável","Motivo"].map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{db.movimentos.map(m=><tr key={m.id}><td className="whitespace-nowrap px-4 py-3">{brDateTime(m.dataHora)}</td><td className="px-4 py-3"><Badge tone={m.tipo==='Saída'||m.tipo==='Ajuste negativo'?'warn':m.tipo==='Estorno'?'danger':'good'}>{m.tipo}</Badge></td><td className="px-4 py-3"><strong>{itemNome(db,m.itemId)}</strong><p className="font-mono text-xs">{db.lotes.find(l=>l.id===m.loteId)?.codigo}</p></td><td className="px-4 py-3">{localNome(db,m.origemId)} → {localNome(db,m.destinoId)}</td><td className="px-4 py-3 font-semibold">{m.quantidade} {m.unidade}</td><td className="px-4 py-3">{m.responsavel}</td><td className="max-w-xs px-4 py-3 text-slate-600">{m.motivo}</td></tr>)}</tbody></table></Card></div>}
