spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/farmacia_viva
    username: farmacia
    password: farmacia
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

logging:
  level:
    br.org.cremic.farmaciaviva: DEBUG
    org.hibernate.SQL: DEBUG

app:
  cors:
    allowed-origins:
      - http://localhost:5173
